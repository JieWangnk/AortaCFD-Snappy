# AortaCFD-Snappy Requirements
# Physics-aware mesh optimization for cardiovascular CFD

# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
pathlib2>=2.3.0; python_version < "3.4"

# Optional dependencies for enhanced functionality
matplotlib>=3.5.0           # For plotting and visualization
pandas>=1.3.0               # For CSV flow data processing (BPM75.csv)

# Development dependencies (optional)
pytest>=6.0.0               # For testing
pytest-cov>=3.0.0           # Coverage reporting
black>=22.0.0               # Code formatting
flake8>=4.0.0               # Linting

# System requirements (must be installed separately):
# - OpenFOAM 12 (Foundation version)
#   Installation: https://openfoam.org/download/
#   Ubuntu: sudo apt-get install openfoam12
#   
# - Python 3.8+ with development headers
#   Ubuntu: sudo apt-get install python3-dev